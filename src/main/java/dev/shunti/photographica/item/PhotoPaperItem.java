package dev.hitom.photographica.item;

import net.minecraft.item.Item;

/** Blank photographic paper used in the Printer and Enlarger blocks. */
public class PhotoPaperItem extends Item {
    public PhotoPaperItem(Settings settings) {
        super(settings);
    }
}
